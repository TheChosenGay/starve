# Binaries
/bin/
*.exe
*.test

# Go
vendor/
coverage.out
*.prof

# 本地环境
.env
.env.*
!.env.example

# 运行产物（存档等）
data/
logs/

# 编辑器 / 系统
.idea/
.vscode/
*.swp
.DS_Store

# 根目录误生成的二进制
/gate

# Go WASM 构建产物（make wasm-collide / wasm-boss / wasm-aggro 生成）
web/collide/collide.wasm
web/collide/boss.wasm
web/collide/aggro.wasm
web/collide/throw.wasm
web/collide/wasm_exec.js

# 误编译出的 bossdemo 二进制
/bossdemo

# 本会话为绕过沙箱限制用的 Go 构建缓存（应在 .gitignore 里）
.gocache*/

# 误编译出的工具二进制：在仓库根目录跑 `go build ./cmd/xxx` 或
# `go build ./tools/xxx` 会产出与目录同名的可执行文件。
#
# 这条规则**不能用通配符**（会误伤同名源码目录），所以改用下面的
# scripts/check-staged-binaries.sh 在提交时自动拦截——
# 逐个列名字的做法已经失败四次（snapprobe / aggrodemo / throw.wasm /
# pomelo-client），说明靠人记不住。
/pomelo-client
/snapprobe
/bossdemo
/aggrodemo
/collidewasm
/modelcollide
