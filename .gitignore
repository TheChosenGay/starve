# Binaries
/bin/
*.exe
*.test

# Go
vendor/
coverage.out
*.prof

# 本地环境
.env
.env.*
!.env.example

# 运行产物（存档等）
data/
logs/

# 编辑器 / 系统
.idea/
.vscode/
*.swp
.DS_Store

# 根目录误生成的二进制
/gate

# Go WASM 构建产物（make wasm-collide / wasm-boss / wasm-aggro 生成）
web/collide/collide.wasm
web/collide/boss.wasm
web/collide/aggro.wasm
web/collide/wasm_exec.js

# 误编译出的 bossdemo 二进制
/bossdemo

# 本会话为绕过沙箱限制用的 Go 构建缓存（应在 .gitignore 里）
.gocache*/

# 误编译出的工具二进制：在仓库根目录跑 `go build ./cmd/xxx` 会产出与目录同名的
# 可执行文件。这里逐个列出（而不是用通配）以免误伤真实源码目录。
# 曾经踩过两次：snapprobe、aggrodemo 各被误提交一次（后者 5.7MB）。
/snapprobe
/bossdemo
/aggrodemo
/collidewasm
/modelcollide
