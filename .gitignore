# Binaries
/bin/
*.exe
*.test

# Go
vendor/
coverage.out
*.prof

# 本地环境
.env
.env.*
!.env.example

# 运行产物（存档等）
data/
logs/

# 编辑器 / 系统
.idea/
.vscode/
*.swp
.DS_Store

# 根目录误生成的二进制
/gate

# Go WASM 构建产物（make wasm-collide / make wasm-boss 生成）
web/collide/collide.wasm
web/collide/boss.wasm
web/collide/wasm_exec.js

# 误编译出的 bossdemo 二进制
/bossdemo

# 本会话为绕过沙箱限制用的 Go 构建缓存（应在 .gitignore 里）
.gocache*/

# 误编译出的 snapprobe 二进制
/snapprobe
